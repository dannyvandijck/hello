apiVersion: v1
kind: Service
metadata:
  name: hello-as-a-service
  namespace: development
spec:
  ports:
    - name: http
      port: 8080
      # target port of the pod you want to reach.
      targetPort: 8080
  type: LoadBalancer    
  selector:
    apps: hello-service